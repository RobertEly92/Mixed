import 'package:flutter/material.dart';

class StatusFormWidget extends StatelessWidget {
  const StatusFormWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}